#include <stdio.h>
#include <stdlib.h>
#include "header/LunarCalendar.h"
#include "header/Login.h"

int main() {
    // FILE *dataFile = fopen("data/users_data.dat", "a");
    // User user = {};
    // printf("%d %s %s %s %d/%d/%d | %s\n", user.id, user.name, 
    //             user.username, user.password, user.dateOfBirth.day,  
    //             user.dateOfBirth.month, user.dateOfBirth.year, (user.name == NULL 
    //                                                             && user.username == NULL
    //                                                             && user.password == NULL) ? "ALL NULL" : "NOT NULL");

    // User user = {};
    // char userName[20];
    // char passWord[20];
    // char strName[50];
    // int strDay = 0;
    // int strMonth = 0;
    // int strYear = 0;
    // printf("Username : ");
    // scanf("%[^\n]", userName);
    // fflush(stdin);
    // printf("Password : ");
    // scanf("%[^\n]", passWord);
    // user.username = userName;
    // user.password = passWord;
    // scanf("%[^\n]", userName);
    
    // printf("Enter username : ");
    // scanf("%s", userName);
    // fflush(stdin);
    // printf("Enter password : ");
    // scanf("%s", passWord);
    // if (adminLogin(userName, passWord) == 1)
    //     printf("Logged in");
    // else if (adminLogin(userName, passWord) == 0)
    //     printf("Wrong password");
    // else
    //     printf("There is no account");

    
    // system("pause");
    return 0;
}
